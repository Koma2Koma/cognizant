<% if current_user %>
  <h3 class='admin-only'><%= link_to 'Edit Category', edit_category_path(@category), class: 'edit-link'%></h3>
<% end %>

<h1><%= @category.title %></h1>
<p><%= @category.description %></p>

<h4>Add intro videos for each category... Maybe</h4>

<div class='container'>
  <% if @topics.any? %>
    <ul>
      <% @topics.each do |topic| %>
        <li><strong><%= link_to topic.title, topic_path(topic) %></strong></li>
          <ul>
            <li><%= topic.description %></li>
          </ul>
      <% end %>
    </ul>
  <% end %>
</div>

<!-- This isn't currently doing anything. -->
<div class='container'>
  <% if @ideas.any? %>
    <ul>
      <% @ideas.each do |idea| %>
        <li><strong><%= link_to idea.title, idea_path(idea) %></strong></li>
          <ul>
            <li><%= idea.description %></li>
          </ul>
      <% end %>
    </ul>
  <% end %>
</div>