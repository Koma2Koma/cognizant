<h1><strong><%= @category.title %></strong> [ <%= @idea.title %> ]</h1>
<p><%= @idea.description %></p>

<%= bootstrap_nested_form_for [@category, @idea] do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :description %>
  <%= f.submit %>
<% end %>
